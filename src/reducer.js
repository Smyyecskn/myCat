export const initialState = {
  loading: false,
  catImage: "",
  error: "",
};

export const reducer = (initialState, action) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
